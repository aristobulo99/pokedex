import { DecimetersToMetersPipe } from './decimeters-to-meters.pipe';

describe('DecimetersToMetersPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimetersToMetersPipe();
    expect(pipe).toBeTruthy();
  });
});
